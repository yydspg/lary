<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.lary.module.group.mapper.InviteItemMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="cn.lary.module.group.entity.InviteItem">
        <id column="id" property="id" />
        <result column="invite_no" property="inviteNo" />
        <result column="group_no" property="groupNo" />
        <result column="inviter" property="inviter" />
        <result column="uid" property="uid" />
        <result column="status" property="status" />
        <result column="create_at" property="createAt" />
        <result column="update_at" property="updateAt" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, invite_no, group_no, inviter, uid, status, create_at, update_at
    </sql>

</mapper>
